
export const setupData = {
    proxyPort: 8500,
    host: 'http://127.0.0.1' //use this if debuging from localhost
    // host: 'http://172.20.10.6'  //Use this configuration to acceess website from another website
}